import React from 'react';
import { Typography, Box } from '@mui/material';

const ClientDetail = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Client Detail
      </Typography>
      <Typography variant="body1">
        Client detail component - Coming soon
      </Typography>
    </Box>
  );
};

export default ClientDetail;